my:
    greeting : Dev
    list:
        values : one,two,three

db:
    connection : "{connectionString: dev}"
    host : 3.0.0.0
    port : 3000

managment.endpoints.web.exposure.include: "*"
spring.application.name: spring-boot-config-Dev
